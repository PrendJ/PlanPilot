# Reference screenshots

The previous PNG previews were removed in v1.1 because they represented the softer-radius geometry of the earlier package.

The canonical visual references are now the live HTML examples in `../ui/` using the v1.1 tokens. Render those examples in a browser to capture fresh screenshots for the target implementation.
