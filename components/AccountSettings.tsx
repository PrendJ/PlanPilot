"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";type Org={id:string;name:string;slug:string;plan:string;role:string};
export function AccountSettings({organizations}:{organizations:Org[]}){const router=useRouter();const[error,setError]=useState("");async function revoke(){await fetch("/api/auth/logout",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({all:true})});router.push("/login");router.refresh()}async function billing(id:string){const r=await fetch("/api/billing/portal",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({organizationId:id})});const b=await r.json();if(r.ok)location.href=b.url;else setError(b.error)}async function remove(org:Org,event:FormEvent<HTMLFormElement>){event.preventDefault();if(!confirm(`Eliminare definitivamente ${org.name}? Prima scarica l'export.`))return;const fd=new FormData(event.currentTarget);const r=await fetch(`/api/organizations/${org.id}`,{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({password:fd.get("password"),confirmSlug:fd.get("confirmSlug")})});const b=await r.json();if(!r.ok)return setError(b.error);router.refresh()}return <div className="settings-stack"><section className="settings-card"><h2>Sicurezza account</h2><p className="muted-copy">Revoca tutte le sessioni, compresa questa.</p><button className="btn" onClick={revoke}>Disconnetti tutti i dispositivi</button></section>{organizations.map(org=><section className="settings-card" key={org.id}><div className="settings-head"><div><h2>{org.name}</h2><p>{org.plan} · {org.role}</p></div><div className="top-actions"><a className="btn" href={`/api/organizations/${org.id}/export`}>Export completo</a>{org.role==="OWNER"&&org.plan!=="TRIAL"&&<button className="btn" onClick={()=>billing(org.id)}>Fatturazione</button>}</div></div>{org.role==="OWNER"&&<details className="danger-zone"><summary>Elimina organizzazione</summary><form onSubmit={e=>remove(org,e)}><p>Operazione irreversibile. Inserisci lo slug <strong>{org.slug}</strong> e la password.</p><input name="confirmSlug" required placeholder={org.slug}/><input name="password" type="password" required placeholder="Password"/><button className="btn danger">Elimina definitivamente</button></form></details>}</section>)}{error&&<div className="status error">{error}</div>}</div>}
