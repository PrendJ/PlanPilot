"use client";import{useSearchParams,useRouter}from"next/navigation";import{useState}from"react";export function AcceptInvite(){const token=useSearchParams().get("token")||"";const router=useRouter();const[error,setError]=useState("");async function accept(){const r=await fetch("/api/invites/accept",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token})});const b=await r.json();if(!r.ok){setError(b.error);if(r.status===401)router.push(`/login?next=${encodeURIComponent(`/accept-invite?token=${token}`)}`);return}router.push("/app");router.refresh()}return <><button className="btn accent" onClick={accept}>Accetta invito</button>{error&&<div className="form-error">{error}</div>}</>}
