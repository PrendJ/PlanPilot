"use client";import { FormEvent,useState } from "react";
export function CheckoutButton({plan,organizationId}:{plan:"SOLO"|"TEAM"|"STUDIO";organizationId?:string}){const[busy,setBusy]=useState(false);async function click(){if(!organizationId){location.href="/register";return}setBusy(true);const r=await fetch("/api/billing/checkout",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({plan,organizationId})});const b=await r.json();setBusy(false);if(r.ok&&b.url)location.href=b.url;else alert(b.error||"Checkout non disponibile")}return <button className="btn accent" onClick={click} disabled={busy}>{busy?"Attendi…":"Inizia"}</button>}
export function EnterpriseForm(){const[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const r=await fetch("/api/sales/enterprise",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form).entries()))});const b=await r.json();if(!r.ok)return setMessage(b.error||"Invio non riuscito");setMessage("Richiesta inviata. Ti ricontatterò personalmente."+(b.bookingUrl?" Puoi anche prenotare subito.":""));if(b.bookingUrl)window.open(b.bookingUrl,"_blank","noopener");form.reset()}return <form className="enterprise-form" onSubmit={submit}><div className="form-grid"><label>Nome<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>Azienda<input name="company" required/></label><label>Dimensione team<input name="teamSize" type="number" min="17" required/></label><label>Lingua<select name="locale" defaultValue="it"><option value="it">Italiano</option><option value="en">English</option><option value="de">Deutsch</option><option value="fr">Français</option><option value="es">Español</option><option value="ru">Русский</option><option value="pl">Polski</option></select></label></div><label>Necessità<textarea name="needs" minLength={10} required/></label><button className="btn accent">Richiedi una call con Lorenzo</button>{message&&<div className="status">{message}</div>}</form>}
