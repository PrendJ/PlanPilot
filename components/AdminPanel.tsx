"use client";

import { Fragment, FormEvent, useMemo, useState } from "react";
import { ExchangeRateControl, type ExchangeRateStatus } from "@/components/ExchangeRateControl";

type Workspace={id:string;name:string;slug:string;lifecycleStatus?:string};
type Membership={role:string;workspace:Workspace};
type OrganizationMembership={role:string;organization:{id:string;name:string;slug:string;plan:string;legalType:string;createdById:string;licenseSource:string;accessExpiresAt:string|Date|null}};
type User={id:string;email:string;name:string;isAdmin:boolean;platformRole?:string;lifecycleStatus?:string;lifetimeFree:boolean;emailVerifiedAt:string|Date|null;memberships:Membership[];organizationMemberships:OrganizationMembership[]};
type Overview={period:string;revenueEur:number;aiCostUsd:number;aiCostEur:number;stripeFeesEur:number;fixedCostsEur:number;contributionEur:number;estimatedNetEur:number;marginPercent:number;organizations:number;companies:number;individuals:number;paidOrganizations:number;lifetimeOrganizations:number;users:number;workspaces:number;aiRequests:number;usdToEur:number;exchangeRate:ExchangeRateStatus};
type OrganizationRow={id:string;name:string;slug:string;legalType:string;plan:string;licenseSource:string;accessExpiresAt:string|Date|null;readOnly:boolean;subscriptionStatus:string|null;members:number;workspaces:number;createdBy:{id:string;name:string;email:string;lifetimeFree:boolean};revenueEur:number;aiCostUsd:number;aiCostEur:number;stripeFeesEur:number;contributionEur:number;aiRequests:number};
type UserRow={id:string;name:string;email:string;isAdmin:boolean;lifetimeFree:boolean;emailVerified:boolean;organizations:number;workspaces:number;aiRequests:number;aiCostUsd:number;aiCostEur:number};
type WorkspaceRow={id:string;name:string;slug:string;organization:{id:string;name:string;plan:string;legalType:string};members:number;cards:number;aiRequests:number;aiCostUsd:number;aiCostEur:number};
type Analytics={overview:Overview;organizations:OrganizationRow[];users:UserRow[];workspaces:WorkspaceRow[];audit:{id:string;action:string;targetType:string;targetId:string;createdAt:string|Date;actor:{name:string;email:string}}[]};
type Section="overview"|"organizations"|"users"|"workspaces";
const sections:{key:Section;label:string;description:string}[]=[{key:"overview",label:"Panoramica",description:"Economics e attività"},{key:"organizations",label:"Organizzazioni",description:"Company e individui"},{key:"users",label:"Utenti",description:"Accessi e licenze"},{key:"workspaces",label:"Workspace",description:"Uso della piattaforma"}];
const manualPlans=["TRIAL","SOLO","TEAM","STUDIO","ENTERPRISE"];
const euro=new Intl.NumberFormat("it-IT",{style:"currency",currency:"EUR",maximumFractionDigits:2});
const usd=new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:4});

export function AdminPanel({initialUsers,workspaces,initialAnalytics,actorRole}:{initialUsers:User[];workspaces:Workspace[];initialAnalytics:Analytics;actorRole:"SUPERADMIN"|"SUPPORT"|"BILLING"|"USER"}){
  const[users,setUsers]=useState(initialUsers);const[analytics,setAnalytics]=useState(initialAnalytics);const[tab,setTab]=useState<Section>("overview");const[query,setQuery]=useState("");const[entityType,setEntityType]=useState("ALL");const[selected,setSelected]=useState<string|null>(null);const[error,setError]=useState("");const[busy,setBusy]=useState(false);
  const superadmin=actorRole==="SUPERADMIN";const support=actorRole==="SUPPORT"||superadmin;
  async function reload(period=analytics.overview.period){const[a,u]=await Promise.all([fetch(`/api/admin/analytics?period=${period}`,{cache:"no-store"}),fetch("/api/admin/users",{cache:"no-store"})]);const ab=await a.json(),ub=await u.json();if(a.ok)setAnalytics(ab);if(u.ok)setUsers(ub.users)}
  async function createUser(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const form=event.currentTarget,data=new FormData(form);const response=await fetch("/api/admin/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:data.get("name"),email:data.get("email"),password:data.get("password"),isAdmin:data.get("isAdmin")==="on",lifetimeFree:data.get("lifetimeFree")==="on"})});const body=await response.json();setBusy(false);if(!response.ok)return setError(body.error||"Creazione non riuscita");form.reset();await reload()}
  async function updateUser(id:string,changes:object){setBusy(true);setError("");const response=await fetch("/api/admin/users",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id,...changes})});const body=await response.json();setBusy(false);if(!response.ok)return setError(body.error||"Aggiornamento non riuscito");if(body.warnings?.length)setError(body.warnings.join(" · "));await reload()}
  async function updateOrganization(id:string,changes:object){setBusy(true);setError("");const response=await fetch(`/api/admin/organizations/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(changes)});const body=await response.json();setBusy(false);if(!response.ok)return setError(body.error||"Aggiornamento non riuscito");await reload()}
  async function assignPlan(event:FormEvent<HTMLFormElement>,organizationId:string){event.preventDefault();setBusy(true);setError("");const data=new FormData(event.currentTarget),rawExpiry=String(data.get("expiresAt")||"");const response=await fetch("/api/admin/licenses",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({organizationId,action:"grant",plan:data.get("plan"),expiresAt:rawExpiry?new Date(`${rawExpiry}T23:59:59.999Z`).toISOString():null,reason:data.get("reason")})});const body=await response.json();setBusy(false);if(!response.ok)return setError(body.error||"Assegnazione non riuscita");await reload()}
  async function assign(userId:string,workspaceId:string){if(!workspaceId)return;const response=await fetch("/api/admin/memberships",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({userId,workspaceId,role:"MEMBER"})});const body=await response.json();if(!response.ok)return setError(body.error||"Assegnazione non riuscita");await reload()}
  async function remove(userId:string,workspaceId:string){const response=await fetch("/api/admin/memberships",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({userId,workspaceId})});const body=await response.json();if(!response.ok)return setError(body.error||"Rimozione non riuscita");await reload()}
  const organizations=useMemo(()=>analytics.organizations.filter(row=>(entityType==="ALL"||row.legalType===entityType)&&`${row.name} ${row.slug} ${row.createdBy.email}`.toLowerCase().includes(query.toLowerCase())),[analytics,query,entityType]);
  const userRows=useMemo(()=>analytics.users.filter(row=>`${row.name} ${row.email}`.toLowerCase().includes(query.toLowerCase())),[analytics,query]);
  const workspaceRows=useMemo(()=>analytics.workspaces.filter(row=>`${row.name} ${row.slug} ${row.organization.name}`.toLowerCase().includes(query.toLowerCase())),[analytics,query]);
  const o=analytics.overview;

  return <div className="backoffice-layout"><aside className="admin-sidebar" aria-label="Menu backoffice"><div className="admin-sidebar-title"><span>CONTROL ROOM</span><strong>{actorRole}</strong></div><nav>{sections.map(item=><button className={tab===item.key?"active":""} key={item.key} onClick={()=>{setTab(item.key);setSelected(null)}}><strong>{item.label}</strong><small>{item.description}</small></button>)}</nav><label>Periodo dati<input type="month" value={o.period} onChange={event=>reload(event.target.value)}/></label></aside><div className="backoffice-content">
    {error&&<div className="status error">{error}<button aria-label="Chiudi errore" onClick={()=>setError("")}>×</button></div>}
    {tab==="overview"&&<><div className="economics-grid"><Metric label="MRR rilevato" value={euro.format(o.revenueEur)} tone="good"/><Metric label="Costo AI" value={euro.format(o.aiCostEur)} sub={usd.format(o.aiCostUsd)}/><Metric label="Commissioni" value={euro.format(o.stripeFeesEur)}/><Metric label="Costi fissi" value={euro.format(o.fixedCostsEur)}/><Metric label="Risultato operativo" value={euro.format(o.contributionEur)} tone={o.contributionEur>=0?"good":"bad"}/><Metric label="Netto stimato" value={euro.format(o.estimatedNetEur)} sub={`${o.marginPercent.toFixed(1)}%`} tone={o.estimatedNetEur>=0?"good":"bad"}/></div><div className="admin-kpis"><Metric label="Organizzazioni" value={String(o.organizations)} sub={`${o.companies} company · ${o.individuals} individui`}/><Metric label="Clienti paid" value={String(o.paidOrganizations)}/><Metric label="Free lifetime" value={String(o.lifetimeOrganizations)}/><Metric label="Utenti" value={String(o.users)}/><Metric label="Workspace" value={String(o.workspaces)}/><Metric label="Richieste AI" value={String(o.aiRequests)}/></div><ExchangeRateControl initial={o.exchangeRate} onChanged={()=>reload()}/><section className="admin-card economics-note"><h2>Come viene calcolato</h2><p>MRR dagli abbonamenti attivi; costo AI dagli eventi effettivi del provider; commissioni stimate al 2,7% + €0,25; costi fissi da configurazione; netto con riserva prudenziale del 30% sull’utile. Cambio applicato: 1 USD = {o.usdToEur} EUR.</p></section><section className="admin-card"><h2>Attività amministrativa recente</h2><div className="audit-list">{analytics.audit.map(item=><div key={item.id}><span className="plan-badge">{item.action.replaceAll("_"," ")}</span><strong>{item.targetType}</strong><small>{item.actor.name} · {new Date(item.createdAt).toLocaleString("it-IT")}</small></div>)}{!analytics.audit.length&&<p className="muted-copy">Nessuna attività registrata.</p>}</div></section></>}
    {tab!=="overview"&&<div className="backoffice-filters"><input type="search" placeholder="Cerca…" value={query} onChange={event=>setQuery(event.target.value)}/>{tab==="organizations"&&<select value={entityType} onChange={event=>setEntityType(event.target.value)}><option value="ALL">Tutti</option><option value="BUSINESS">Company</option><option value="PERSONAL">Individui</option></select>}</div>}
    {tab==="organizations"&&<section className="admin-card table-card"><table className="backoffice-table"><thead><tr><th>Organizzazione</th><th>Piano</th><th>Membri / WS</th><th>MRR</th><th>Costo AI</th><th>Contributo</th><th></th></tr></thead><tbody>{organizations.map(row=><Fragment key={row.id}><tr className={selected===row.id?"selected":""}><td><button className="table-entity" onClick={()=>setSelected(selected===row.id?null:row.id)}><strong>{row.name}</strong><small>{row.legalType==="PERSONAL"?"Individuo":"Company"} · {row.createdBy.email}</small></button></td><td><span className={`plan-badge plan-${row.plan}`}>{row.plan}</span><small className="block">{row.licenseSource}</small></td><td>{row.members} / {row.workspaces}</td><td>{euro.format(row.revenueEur)}</td><td>{euro.format(row.aiCostEur)}</td><td className={row.contributionEur>=0?"positive":"negative"}>{euro.format(row.contributionEur)}</td><td><button className="btn" onClick={()=>setSelected(selected===row.id?null:row.id)}>Dettagli</button></td></tr>{selected===row.id&&<tr className="drilldown-row"><td colSpan={7}><div className="drilldown"><div><span>Richieste AI</span><strong>{row.aiRequests}</strong></div><div><span>Costi provider</span><strong>{usd.format(row.aiCostUsd)}</strong></div><div><span>Stato billing</span><strong>{row.subscriptionStatus||row.licenseSource}</strong></div>{row.accessExpiresAt&&<div><span>Scadenza</span><strong>{new Date(row.accessExpiresAt).toLocaleDateString("it-IT")}</strong></div>}{superadmin&&<><label>Tipo<select value={row.legalType} disabled={busy} onChange={event=>updateOrganization(row.id,{legalType:event.target.value})}><option value="BUSINESS">Company</option><option value="PERSONAL">Individuo</option></select></label><button className={`btn ${row.readOnly?"accent":"danger"}`} disabled={busy} onClick={()=>updateOrganization(row.id,{readOnly:!row.readOnly})}>{row.readOnly?"Riattiva":"Sola lettura"}</button></>}</div></td></tr>}</Fragment>)}</tbody></table></section>}
    {tab==="users"&&<>{superadmin&&<form className="admin-card create-admin-user" onSubmit={createUser}><div><h2>Crea utente</h2><p>Email verificata e organizzazione personale incluse.</p></div><input name="name" placeholder="Nome" required/><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" minLength={10} placeholder="Password iniziale" required/><label><input type="checkbox" name="lifetimeFree"/> Free a vita</label><label><input type="checkbox" name="isAdmin"/> Superadmin</label><button className="btn accent" disabled={busy}>Crea</button></form>}<section className="admin-card table-card"><table className="backoffice-table"><thead><tr><th>Utente</th><th>Accesso</th><th>Org / WS</th><th>Richieste AI</th><th>Costo AI</th><th></th></tr></thead><tbody>{userRows.map(row=>{const user=users.find(item=>item.id===row.id);const personal=user?.organizationMemberships.find(item=>item.organization.createdById===row.id&&item.organization.legalType==="PERSONAL");return <Fragment key={row.id}><tr><td><button className="table-entity" onClick={()=>setSelected(selected===row.id?null:row.id)}><strong>{row.name}</strong><small>{row.email}</small></button></td><td>{row.lifetimeFree?<span className="plan-badge plan-LIFETIME">FREE A VITA</span>:row.isAdmin?"SUPERADMIN":"Standard"}</td><td>{row.organizations} / {row.workspaces}</td><td>{row.aiRequests}</td><td>{euro.format(row.aiCostEur)}</td><td><button className="btn" onClick={()=>setSelected(selected===row.id?null:row.id)}>Gestisci</button></td></tr>{selected===row.id&&<tr className="drilldown-row"><td colSpan={6}><div className="user-management-panel">{superadmin&&<div className="drilldown user-controls"><label><input type="checkbox" checked={row.lifetimeFree} onChange={event=>{const enabled=event.target.checked;if(confirm(enabled?"Concedere accesso gratuito a vita? Gli abbonamenti attivi saranno impostati per non rinnovarsi.":"Revocare l'accesso gratuito a vita? Le organizzazioni senza abbonamento avranno 7 giorni di prova."))updateUser(row.id,{lifetimeFree:enabled})}}/> Free a vita</label><label><input type="checkbox" checked={row.isAdmin} onChange={event=>updateUser(row.id,{isAdmin:event.target.checked})}/> Superadmin</label><label><input type="checkbox" checked={row.emailVerified} onChange={event=>updateUser(row.id,{emailVerified:event.target.checked})}/> Email verificata</label><span>Costo provider: {usd.format(row.aiCostUsd)}</span></div>}{superadmin&&(personal?<form className="personal-plan-form" onSubmit={event=>assignPlan(event,personal.organization.id)}><div><span className="eyebrow">PIANO ORGANIZZAZIONE PERSONALE</span><strong>{personal.organization.name}</strong><small>Attuale: {personal.organization.plan} · {personal.organization.licenseSource}</small></div><label>Piano<select name="plan" defaultValue={manualPlans.includes(personal.organization.plan)?personal.organization.plan:"SOLO"}>{manualPlans.map(plan=><option key={plan}>{plan}</option>)}</select></label><label>Scadenza opzionale<input name="expiresAt" type="date"/></label><label className="plan-reason">Motivazione<input name="reason" minLength={4} maxLength={1000} required placeholder="Motivo dell’assegnazione"/></label><button className="btn accent" disabled={busy}>Assegna piano</button></form>:<div className="personal-plan-missing"><strong>Nessuna organizzazione personale</strong><span>Il piano non può essere assegnato implicitamente.</span></div>)}{support&&<WorkspaceMemberships user={user} workspaces={workspaces} assign={assign} remove={remove}/>}</div></td></tr>}</Fragment>})}</tbody></table></section></>}
    {tab==="workspaces"&&<section className="admin-card table-card"><table className="backoffice-table"><thead><tr><th>Workspace</th><th>Organizzazione</th><th>Membri</th><th>Card</th><th>Richieste AI</th><th>Costo AI</th></tr></thead><tbody>{workspaceRows.map(row=><tr key={row.id}><td><span className="table-entity"><strong>{row.name}</strong><small>{row.slug}</small></span></td><td>{row.organization.name}<small className="block">{row.organization.plan}</small></td><td>{row.members}</td><td>{row.cards}</td><td>{row.aiRequests}</td><td>{euro.format(row.aiCostEur)}<small className="block">{usd.format(row.aiCostUsd)}</small></td></tr>)}</tbody></table></section>}
  </div></div>;
}

function Metric({label,value,sub,tone}:{label:string;value:string;sub?:string;tone?:string}){return <article className={`metric-card ${tone||""}`}><span>{label}</span><strong>{value}</strong>{sub&&<small>{sub}</small>}</article>}
function WorkspaceMemberships({user,workspaces,assign,remove}:{user:User|undefined;workspaces:Workspace[];assign:(userId:string,workspaceId:string)=>void;remove:(userId:string,workspaceId:string)=>void}){if(!user)return null;return <div className="admin-memberships"><div>{user.memberships.map(membership=><span key={membership.workspace.id}>{membership.workspace.name} · {membership.role}<button aria-label={`Rimuovi ${membership.workspace.name}`} onClick={()=>remove(user.id,membership.workspace.id)}>×</button></span>)}</div><select defaultValue="" onChange={event=>{assign(user.id,event.target.value);event.currentTarget.value=""}}><option value="">+ Assegna workspace</option>{workspaces.filter(workspace=>!user.memberships.some(membership=>membership.workspace.id===workspace.id)).map(workspace=><option value={workspace.id} key={workspace.id}>{workspace.name}</option>)}</select></div>}
