import { describe,expect,it,vi } from "vitest";import { planPatchFromText } from "@/lib/openrouter";
describe("OpenRouter privacy routing",()=>{it("requires ZDR and disables collection and fallback",async()=>{const mocked=vi.fn().mockResolvedValue({ok:true,json:async()=>({id:"req_1",choices:[{message:{content:JSON.stringify({summary:"ok",actions:[]})}}],usage:{cost:.001}})});vi.stubGlobal("fetch",mocked);await planPatchFromText({apiKey:"test",model:"openai/gpt-5-nano",workspaceName:"Test",userText:"Nessuna modifica",plan:{columns:[]}});const body=JSON.parse(mocked.mock.calls[0][1].body);expect(body.provider).toEqual({require_parameters:true,data_collection:"deny",zdr:true,allow_fallbacks:false})})});
