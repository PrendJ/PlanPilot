import { NextResponse } from "next/server";import { z } from "zod";import { sendEmail } from "@/lib/email";import { clientIp,rateLimit,rejectCrossOrigin } from "@/lib/security";
const schema=z.object({name:z.string().trim().min(2).max(100),email:z.string().email(),company:z.string().trim().min(2).max(150),teamSize:z.coerce.number().int().min(17).max(100000),needs:z.string().trim().min(10).max(5000),locale:z.enum(["it","en","de","fr","es","ru","pl"])});
export async function POST(request:Request){const originError=rejectCrossOrigin(request);if(originError)return originError;const limited=rateLimit(`sales:${clientIp(request)}`,5,60*60_000);if(limited)return limited;const parsed=schema.safeParse(await request.json().catch(()=>({})));if(!parsed.success)return NextResponse.json({error:"Controlla i dati inseriti"},{status:400});const b=parsed.data;const to=process.env.SALES_EMAIL;if(!to)return NextResponse.json({error:"Contatto commerciale non configurato"},{status:503});await sendEmail({to,subject:`Richiesta Enterprise · ${b.company}`,html:`<h2>Nuova richiesta Enterprise</h2><p><strong>Nome:</strong> ${b.name}</p><p><strong>Email:</strong> ${b.email}</p><p><strong>Azienda:</strong> ${b.company}</p><p><strong>Team:</strong> ${b.teamSize}</p><p><strong>Lingua:</strong> ${b.locale}</p><p><strong>Necessità:</strong></p><p>${b.needs.replace(/[<>&]/g,"")}</p>`});return NextResponse.json({ok:true,bookingUrl:process.env.SALES_BOOKING_URL||null})}
