import { NextResponse } from "next/server";import { prisma } from "@/lib/prisma";import { refreshUsdToEurRate } from "@/lib/exchange-rate";import { runAccountEmailLifecycle, runRetention } from "@/lib/lifecycle";
export async function POST(request:Request){const secret=process.env.CRON_SECRET;if(!secret||request.headers.get("authorization")!==`Bearer ${secret}`)return NextResponse.json({error:"Unauthorized"},{status:401});const lifecycle=await runAccountEmailLifecycle(request);const retention=await runRetention();await prisma.verificationToken.deleteMany({where:{expiresAt:{lt:new Date()}}});await prisma.passwordResetToken.deleteMany({where:{OR:[{expiresAt:{lt:new Date()}},{usedAt:{not:null}}]}});await prisma.workspaceInvite.deleteMany({where:{OR:[{expiresAt:{lt:new Date()}},{revokedAt:{not:null}}]}});await prisma.session.deleteMany({where:{expiresAt:{lt:new Date()}}});const exchangeRate=await refreshUsdToEurRate();return NextResponse.json({...lifecycle,...retention,exchangeRate})}
