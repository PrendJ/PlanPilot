import { NextResponse } from "next/server";import { getCurrentUser,getOrganizationAccess } from "@/lib/auth";import { getUsageStatus } from "@/lib/plans";
export async function GET(_:Request,{params}:{params:Promise<{organizationId:string}>}){const user=await getCurrentUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const {organizationId}=await params;if(!(await getOrganizationAccess(user.id,organizationId)))return NextResponse.json({error:"Not found"},{status:404});const usage=await getUsageStatus(organizationId);return NextResponse.json(usage?{percent:usage.percent,status:usage.status,resetsAt:usage.resetsAt}:null)}
