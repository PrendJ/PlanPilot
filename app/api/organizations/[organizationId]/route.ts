import bcrypt from "bcryptjs";import { NextResponse } from "next/server";import { z } from "zod";import { getCurrentUser,getOrganizationAccess } from "@/lib/auth";import { prisma } from "@/lib/prisma";import { rejectCrossOrigin } from "@/lib/security";
export async function DELETE(request:Request,{params}:{params:Promise<{organizationId:string}>}){const user=await getCurrentUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const originError=rejectCrossOrigin(request);if(originError)return originError;const{organizationId}=await params;const access=await getOrganizationAccess(user.id,organizationId);if(!access||access.role!=="OWNER")return NextResponse.json({error:"Forbidden"},{status:403});const parsed=z.object({password:z.string().min(8),confirmSlug:z.string()}).safeParse(await request.json().catch(()=>({})));if(!parsed.success||parsed.data.confirmSlug!==access.organization.slug)return NextResponse.json({error:"Conferma non valida"},{status:400});if(!(await bcrypt.compare(parsed.data.password,user.passwordHash)))return NextResponse.json({error:"Password non valida"},{status:403});await prisma.organization.delete({where:{id:organizationId}});return NextResponse.json({ok:true})}
