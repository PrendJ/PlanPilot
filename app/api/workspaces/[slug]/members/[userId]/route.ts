import { NextResponse } from "next/server";import { z } from "zod";import { getCurrentUser } from "@/lib/auth";import { prisma } from "@/lib/prisma";import { workspaceForUser } from "@/lib/board";
export async function PATCH(request:Request,{params}:{params:Promise<{slug:string;userId:string}>}){const user=await getCurrentUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const{slug,userId}=await params;const workspace=await workspaceForUser(slug,user.id);if(!workspace)return NextResponse.json({error:"Not found"},{status:404});const actor=await prisma.workspaceMember.findUnique({where:{workspaceId_userId:{workspaceId:workspace.id,userId:user.id}}});if(!user.isAdmin&&actor?.role!=="OWNER")return NextResponse.json({error:"Only the workspace owner can change roles"},{status:403});const parsed=z.object({role:z.enum(["OWNER","ADMIN","MEMBER"])}).safeParse(await request.json().catch(()=>({})));if(!parsed.success)return NextResponse.json({error:"Invalid role"},{status:400});const target=await prisma.workspaceMember.findUnique({where:{workspaceId_userId:{workspaceId:workspace.id,userId}}});if(!target)return NextResponse.json({error:"Member not found"},{status:404});if(target.role==="OWNER"&&parsed.data.role!=="OWNER"&&await prisma.workspaceMember.count({where:{workspaceId:workspace.id,role:"OWNER"}})<=1)return NextResponse.json({error:"A workspace must keep at least one owner"},{status:400});const membership=await prisma.workspaceMember.update({where:{id:target.id},data:{role:parsed.data.role}});return NextResponse.json({membership})}
export async function DELETE(_:Request,{params}:{params:Promise<{slug:string;userId:string}>}){const user=await getCurrentUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const{slug,userId}=await params;const workspace=await workspaceForUser(slug,user.id);if(!workspace)return NextResponse.json({error:"Not found"},{status:404});const actor=await prisma.workspaceMember.findUnique({where:{workspaceId_userId:{workspaceId:workspace.id,userId:user.id}}});if(!user.isAdmin&&actor?.role!=="OWNER")return NextResponse.json({error:"Only the workspace owner can remove members"},{status:403});const target=await prisma.workspaceMember.findUnique({where:{workspaceId_userId:{workspaceId:workspace.id,userId}}});if(!target)return NextResponse.json({error:"Member not found"},{status:404});if(target.role==="OWNER"&&await prisma.workspaceMember.count({where:{workspaceId:workspace.id,role:"OWNER"}})<=1)return NextResponse.json({error:"The last owner cannot be removed"},{status:400});await prisma.workspaceMember.delete({where:{id:target.id}});return NextResponse.json({ok:true})}
