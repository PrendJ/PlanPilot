import { NextResponse } from "next/server";import { z } from "zod";import { getCurrentUser,canManageWorkspace,createOpaqueToken,hashToken } from "@/lib/auth";import { prisma } from "@/lib/prisma";import { workspaceForUser } from "@/lib/board";import { PLANS,planKey } from "@/lib/plans";import { appUrl,sendEmail } from "@/lib/email";import { rateLimit,rejectCrossOrigin } from "@/lib/security";
export async function POST(request:Request,{params}:{params:Promise<{slug:string}>}){const user=await getCurrentUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const originError=rejectCrossOrigin(request);if(originError)return originError;const limited=rateLimit(`invite:${user.id}`,20,60*60_000);if(limited)return limited;const{slug}=await params;const workspace=await workspaceForUser(slug,user.id);if(!workspace||!(await canManageWorkspace(user.id,workspace.id,user.isAdmin)))return NextResponse.json({error:"Forbidden"},{status:403});const parsed=z.object({email:z.string().email(),role:z.enum(["ADMIN","MEMBER"]).default("MEMBER")}).safeParse(await request.json().catch(()=>({})));if(!parsed.success)return NextResponse.json({error:"Invito non valido"},{status:400});const email=parsed.data.email.toLowerCase();const existingUser=await prisma.user.findUnique({where:{email},select:{id:true}});const alreadyInOrganization=existingUser?await prisma.organizationMember.findUnique({where:{organizationId_userId:{organizationId:workspace.organizationId,userId:existingUser.id}},select:{id:true}}):null;const limit=PLANS[planKey(workspace.organization.plan)].memberLimit;const members=await prisma.organizationMember.count({where:{organizationId:workspace.organizationId}});if(!alreadyInOrganization&&members>=limit)return NextResponse.json({error:"Limite membri del piano raggiunto"},{status:402});const token=createOpaqueToken();const invite=await prisma.workspaceInvite.create({data:{organizationId:workspace.organizationId,workspaceId:workspace.id,invitedById:user.id,email,role:parsed.data.role,tokenHash:hashToken(token),expiresAt:new Date(Date.now()+7*86400000)}});await sendEmail({to:invite.email,subject:`Invito a ${workspace.name} su BoardCue`,html:`<p>${user.name} ti ha invitato nel workspace <strong>${workspace.name}</strong>.</p><p><a href="${appUrl()}/accept-invite?token=${encodeURIComponent(token)}">Accetta l'invito</a></p><p>Il link scade tra 7 giorni.</p>`});return NextResponse.json({invite:{id:invite.id,email:invite.email,role:invite.role,expiresAt:invite.expiresAt}},{status:201})}
